#include "config.h"

// Definizione delle variabili globali

int NUM_SHAPES = 0;
int current_policy = 0;
bool policy_selected = false;
int num_cores = 1; // Default to 1 core

MenuState menu_state = SELECT_ALGORITHM;
